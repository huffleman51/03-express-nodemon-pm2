/* eslint-disable no-console */
// @flow

import Dog from './Dog'

const toby = new Dog('Toby')

console.log(toby.bark())

// Used to test the Compact plugin, this feature is not supported in all browsers

// const workerContainerInstance = navigator.serviceWorker

// console.log(workerContainerInstance)
